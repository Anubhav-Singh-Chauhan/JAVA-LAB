package Assignment_01;

public class Q18 {
    public static void main(String[] args) {
        /*
         Local static:
       ->Default value is zero
       ->Stored in data section(bss or initialized) of memory
       ->Scope or visibility is within the block or a function
       ->Life of local static variable starts and only ends with the program.

        Global static:
     ->If a global variable is static, the scope or visibility is within that file only,
       even using extern in another file won’t work. It gives linkage error.
         */
    }
}
